//This function returns a string of each number from 1 to 10 being multiplied by number

function multiTable(number) {
  let a = 1 * number;
  let b = 2 * number;
  let c = 3 * number;
  let d = 4 * number;
  let e = 5 * number;
  let f = 6 * number;
  let g = 7 * number;
  let h = 8 * number;
  let i = 9 * number;
  let j = 10 * number;
  return (
    "1 * " +
    number +
    " = " +
    a +
    "\n2 * " +
    number +
    " = " +
    b +
    "\n3 * " +
    number +
    " = " +
    c +
    "\n4 * " +
    number +
    " = " +
    d +
    "\n5 * " +
    number +
    " = " +
    e +
    "\n6 * " +
    number +
    " = " +
    f +
    "\n7 * " +
    number +
    " = " +
    g +
    "\n8 * " +
    number +
    " = " +
    h +
    "\n9 * " +
    number +
    " = " +
    i +
    "\n10 * " +
    number +
    " = " +
    j
  );
}
